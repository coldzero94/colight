// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/coby/colight/apps/backend/ent/aicallerror"
	"github.com/coby/colight/apps/backend/ent/usagelog"
	"github.com/google/uuid"
)

// AICallError is the model entity for the AICallError schema.
type AICallError struct {
	config `json:"-"`
	// ID of the ent.
	// Primary key
	ID uuid.UUID `json:"id,omitempty"`
	// Record creation timestamp
	CreatedAt time.Time `json:"created_at,omitempty"`
	// 1:1 FK to usage_logs — CASCADE on delete
	UsageLogID uuid.UUID `json:"usage_log_id,omitempty"`
	// Classified AI error category
	ErrorType aicallerror.ErrorType `json:"error_type,omitempty"`
	// Raw error message from provider
	ErrorMessage string `json:"error_message,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the AICallErrorQuery when eager-loading is set.
	Edges        AICallErrorEdges `json:"edges"`
	selectValues sql.SelectValues
}

// AICallErrorEdges holds the relations/edges for other nodes in the graph.
type AICallErrorEdges struct {
	// UsageLog holds the value of the usage_log edge.
	UsageLog *UsageLog `json:"usage_log,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// UsageLogOrErr returns the UsageLog value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e AICallErrorEdges) UsageLogOrErr() (*UsageLog, error) {
	if e.UsageLog != nil {
		return e.UsageLog, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: usagelog.Label}
	}
	return nil, &NotLoadedError{edge: "usage_log"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*AICallError) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case aicallerror.FieldErrorType, aicallerror.FieldErrorMessage:
			values[i] = new(sql.NullString)
		case aicallerror.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		case aicallerror.FieldID, aicallerror.FieldUsageLogID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the AICallError fields.
func (_m *AICallError) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case aicallerror.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case aicallerror.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case aicallerror.FieldUsageLogID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field usage_log_id", values[i])
			} else if value != nil {
				_m.UsageLogID = *value
			}
		case aicallerror.FieldErrorType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field error_type", values[i])
			} else if value.Valid {
				_m.ErrorType = aicallerror.ErrorType(value.String)
			}
		case aicallerror.FieldErrorMessage:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field error_message", values[i])
			} else if value.Valid {
				_m.ErrorMessage = value.String
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the AICallError.
// This includes values selected through modifiers, order, etc.
func (_m *AICallError) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryUsageLog queries the "usage_log" edge of the AICallError entity.
func (_m *AICallError) QueryUsageLog() *UsageLogQuery {
	return NewAICallErrorClient(_m.config).QueryUsageLog(_m)
}

// Update returns a builder for updating this AICallError.
// Note that you need to call AICallError.Unwrap() before calling this method if this AICallError
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *AICallError) Update() *AICallErrorUpdateOne {
	return NewAICallErrorClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the AICallError entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *AICallError) Unwrap() *AICallError {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: AICallError is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *AICallError) String() string {
	var builder strings.Builder
	builder.WriteString("AICallError(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("usage_log_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.UsageLogID))
	builder.WriteString(", ")
	builder.WriteString("error_type=")
	builder.WriteString(fmt.Sprintf("%v", _m.ErrorType))
	builder.WriteString(", ")
	builder.WriteString("error_message=")
	builder.WriteString(_m.ErrorMessage)
	builder.WriteByte(')')
	return builder.String()
}

// AICallErrors is a parsable slice of AICallError.
type AICallErrors []*AICallError
