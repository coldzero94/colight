// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/coby/colight/apps/backend/ent/experience"
	"github.com/coby/colight/apps/backend/ent/experiencetag"
	"github.com/google/uuid"
)

// ExperienceTag is the model entity for the ExperienceTag schema.
type ExperienceTag struct {
	config `json:"-"`
	// ID of the ent.
	// Primary key
	ID uuid.UUID `json:"id,omitempty"`
	// Record creation timestamp
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Tag name (competency name)
	TagName string `json:"tag_name,omitempty"`
	// Tag type: skill, soft_skill, industry, keyword
	TagType string `json:"tag_type,omitempty"`
	// AI classification confidence (0~1)
	Confidence float64 `json:"confidence,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ExperienceTagQuery when eager-loading is set.
	Edges           ExperienceTagEdges `json:"edges"`
	experience_tags *uuid.UUID
	selectValues    sql.SelectValues
}

// ExperienceTagEdges holds the relations/edges for other nodes in the graph.
type ExperienceTagEdges struct {
	// Experience holds the value of the experience edge.
	Experience *Experience `json:"experience,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// ExperienceOrErr returns the Experience value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ExperienceTagEdges) ExperienceOrErr() (*Experience, error) {
	if e.Experience != nil {
		return e.Experience, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: experience.Label}
	}
	return nil, &NotLoadedError{edge: "experience"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ExperienceTag) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case experiencetag.FieldConfidence:
			values[i] = new(sql.NullFloat64)
		case experiencetag.FieldTagName, experiencetag.FieldTagType:
			values[i] = new(sql.NullString)
		case experiencetag.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		case experiencetag.FieldID:
			values[i] = new(uuid.UUID)
		case experiencetag.ForeignKeys[0]: // experience_tags
			values[i] = &sql.NullScanner{S: new(uuid.UUID)}
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ExperienceTag fields.
func (_m *ExperienceTag) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case experiencetag.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case experiencetag.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case experiencetag.FieldTagName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field tag_name", values[i])
			} else if value.Valid {
				_m.TagName = value.String
			}
		case experiencetag.FieldTagType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field tag_type", values[i])
			} else if value.Valid {
				_m.TagType = value.String
			}
		case experiencetag.FieldConfidence:
			if value, ok := values[i].(*sql.NullFloat64); !ok {
				return fmt.Errorf("unexpected type %T for field confidence", values[i])
			} else if value.Valid {
				_m.Confidence = value.Float64
			}
		case experiencetag.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field experience_tags", values[i])
			} else if value.Valid {
				_m.experience_tags = new(uuid.UUID)
				*_m.experience_tags = *value.S.(*uuid.UUID)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the ExperienceTag.
// This includes values selected through modifiers, order, etc.
func (_m *ExperienceTag) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryExperience queries the "experience" edge of the ExperienceTag entity.
func (_m *ExperienceTag) QueryExperience() *ExperienceQuery {
	return NewExperienceTagClient(_m.config).QueryExperience(_m)
}

// Update returns a builder for updating this ExperienceTag.
// Note that you need to call ExperienceTag.Unwrap() before calling this method if this ExperienceTag
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *ExperienceTag) Update() *ExperienceTagUpdateOne {
	return NewExperienceTagClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the ExperienceTag entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *ExperienceTag) Unwrap() *ExperienceTag {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: ExperienceTag is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *ExperienceTag) String() string {
	var builder strings.Builder
	builder.WriteString("ExperienceTag(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("tag_name=")
	builder.WriteString(_m.TagName)
	builder.WriteString(", ")
	builder.WriteString("tag_type=")
	builder.WriteString(_m.TagType)
	builder.WriteString(", ")
	builder.WriteString("confidence=")
	builder.WriteString(fmt.Sprintf("%v", _m.Confidence))
	builder.WriteByte(')')
	return builder.String()
}

// ExperienceTags is a parsable slice of ExperienceTag.
type ExperienceTags []*ExperienceTag
